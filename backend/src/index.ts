/**
 * Entry point — starts the HTTP server defined in app.ts.
 *
 * All application setup (middleware, routes, versioning, Socket.IO) lives in
 * app.ts so that it can be imported by tests without side-effects.
 */
import './app'
